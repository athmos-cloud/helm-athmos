host: "registry.athmos-cloud.com"
resolverName: "main-resolver"
registry:
  certSecretName: registry-cert-secret
  namespace: dev
  services:
    portal:
      name: harbor-portal
      port: 80
    core:
      name: harbor-core
      port: 80
